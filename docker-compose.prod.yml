services:
  backend:
    build:
      target: production
    environment:
      NODE_ENV: production
    restart: unless-stopped

  frontend:
    build:
      target: production
    ports:
      - "80:80"
    restart: unless-stopped

  scraper:
    build:
      target: production
    restart: no

  postgres:
    restart: unless-stopped